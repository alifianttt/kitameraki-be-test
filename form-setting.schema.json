{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FormSettings",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Cosmos DB item id — mirrors organizationId (one document per organisation)"
    },
    "organizationId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier of the organisation these settings belong to"
    },
    "settings": {
      "type": "object",
      "description": "Arbitrary key/value pairs representing the form configuration",
      "additionalProperties": true
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of the last upsert, set server-side"
    }
  },
  "required": ["id", "organizationId", "settings", "updatedAt"],
  "additionalProperties": false
}